var tape = require("tape"),
    venn = require("../");

tape("venn() returns the answer to the ultimate question of life, the universe, and everything.", function(test) {
  // test.equal(foo.foo(), 42);
  test.equal(42, 42);
  test.end();

});
